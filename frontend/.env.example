# Copy to .env.local for local development (never commit secrets).

# Optional — enable Supabase magic link + push sync from the Data tab
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional — shows experimental segmentation banner in Scan > analysis (no model bundled)
NEXT_PUBLIC_ENABLE_UNET=
